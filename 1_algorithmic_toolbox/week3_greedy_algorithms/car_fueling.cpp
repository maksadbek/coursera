#include <iostream>#include <vector>/* * the greedy choice is choosing the furthest gas station * save move is */int min_refills(std::vector<int> fuel_stations, int l) {	int refills = 0;	int current_station = 0;	while(current_station < fuel_stations.length()) {		int last_station = current_station;		int next_station = fuel_stations[current_station+1];		while(current_station <= fuel_stations.length() && (next_station - last_refill_station) < l) {			current_station = next_station;			next_station = fuel_stations[current_station+1];		}		if(current_station == last_station) {			return -1;		}		if(current_station <= fuel_stations.lenth()) {			refills++;		}	}	return refills;}int main() {	auto stations = new vector<int>();	stations.push_back(1)	min_refills()}